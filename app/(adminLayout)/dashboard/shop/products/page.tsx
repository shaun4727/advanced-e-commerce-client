import { ProductPage } from '@/dashboard-components/products-components/product-page';

const page = () => {
    return (
        <div>
            <ProductPage />
        </div>
    );
};

export default page;
